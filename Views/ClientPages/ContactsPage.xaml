<Page
    x:Class="VoiceX.Views.ClientPages.ContactsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VoiceX.Views.ClientPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid RenderTransform="{DynamicResource GlobalFlip}" RenderTransformOrigin="{DynamicResource GlobalOrigin}" VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,2,0,0">
            <TextBox TextChanged="Search_TextChanged" BorderThickness="0, 0, 0, 1" x:Name="Search"  MinHeight="26" Width="225" BorderBrush="#FFD8D8FE" VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="14" Height="25" Padding="6,0,6,0"/>
            <Grid HorizontalAlignment="Right" Margin="0,0,7,0">
                <Border CornerRadius="0.5" x:Name="magnifyingGlassLine" Background="#FFBFBDFC" Height="1.1" Width="6" RenderTransformOrigin="0.5,0.5" Margin="8,8.6,0,0">
                    <Border.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform/>
                            <SkewTransform/>
                            <RotateTransform Angle="45"/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </Border.RenderTransform>
                </Border>
                <Ellipse x:Name="magnifyingGlassEllipse" Fill="White" StrokeThickness="1" Width="10" Height="11" Stroke="#FFBFBDFC"/>
            </Grid>
        </Grid>

        <ListBox ScrollViewer.VerticalScrollBarVisibility="Hidden"  ScrollViewer.HorizontalScrollBarVisibility="Hidden" BorderThickness="0" x:Name="ContactsList" MaxWidth="500" Margin="10,30,0,30" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="White" Padding="0,0,10,0"/>

    </Grid>
</Page>
