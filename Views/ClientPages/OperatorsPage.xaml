<Page
    x:Class="VoiceX.Views.ClientPages.OperatorsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VoiceX.Views.ClientPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid>
        <Border VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,2,0,0" BorderThickness="0, 0, 0, 1" BorderBrush="#FFDADADB">
            <Grid>
                <TextBox BorderThickness="0" Cursor="Hand" x:Name="Search"  MinHeight="26" TextChanged="Search_TextChanged" Width="225" VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="14" Height="25" Padding="6,0,6,0"/>
                <Grid Height="15" x:Name="magnifyingGlass" Width="15" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,3">
                    <Rectangle x:Name="magnifyingGlassLine" Fill="#FF898989" Height="1.1" Width="6" RadiusX="1" RadiusY="1" RenderTransformOrigin="0.5,0.5" Margin="8,8.6,0,0">
                        <Rectangle.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform/>
                                <SkewTransform/>
                                <RotateTransform Angle="45"/>
                                <TranslateTransform/>
                            </TransformGroup>
                        </Rectangle.RenderTransform>

                    </Rectangle>
                    <Ellipse x:Name="magnifyingGlassEllipse" Fill="White" StrokeThickness="1" Width="10" Height="11" Stroke="#FF898989"/>
                </Grid>
            </Grid>
        </Border>
        <ListBox ScrollViewer.VerticalScrollBarVisibility="Hidden" Height="243" ScrollViewer.HorizontalScrollBarVisibility="Hidden" x:Name="ContactsList" BorderThickness="0" Margin="13,29,0,0" HorizontalAlignment="Stretch" VerticalAlignment="Top" Background="White" Padding="0,0,0,0"/>
    </Grid>
</Page>
