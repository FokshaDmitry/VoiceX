<Page
    x:Class="VoiceX.Views.ControlPages.ClickToCallPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VoiceX.Views.ControlPages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="White" Name="ClickToCall" Loaded="ClickToCall_Loaded" MaxWidth="500">
    <Page.Resources>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Cursor="Hand" Background="Transparent" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid VerticalAlignment="Top">
            <TextBlock Text="Dail Transform" x:Name="DailText" FontFamily="Nunito Sans 10pt Normal" Foreground="#FF252422" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="16,5,0,0" FontSize="16"/>
            <TextBox BorderThickness="1" Cursor="Hand" ToolTipService.ToolTip="Change control combination" ToolTipService.Placement="Mouse" Background="Transparent" x:Name="HotKeyBox" MaxHeight="40" IsReadOnly="True" KeyDown="HotKeyBox_KeyDown" KeyUp="HotKeyBox_KeyUp" FontFamily="Nunito Sans 10pt Normal" MinHeight="20" HorizontalAlignment="Stretch"  TextWrapping="Wrap" VerticalAlignment="Top" FontSize="14" Height="24" Margin="16,27,16,0" MinWidth="150" FontWeight="Normal" Padding="6,1,6,0" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" Foreground="#FF252422" BorderBrush="#FFBBBAFF"/>
            <Image Width="14" Height="14" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,25,5">
                <Image.Source>
                    <BitmapImage UriSource="/Assets/Icone_v2/Reload.png" DecodePixelWidth="50" DecodePixelHeight="50"/>
                </Image.Source>
            </Image>
        </Grid>
        <Grid Margin="0,55,0,0" Height="250" VerticalAlignment="Top">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="10*"/>
            </Grid.RowDefinitions>
            <Grid x:Name="SectionGrig" Grid.Row="0">
                <TextBlock Text="Enter Data" Margin="16,0,0,0" FontFamily="Nunito Sans 10pt Normal"/>
                <Button Cursor="Hand" Click="Remove_Click" Height="16" Padding="0,0,0,0" BorderBrush="Transparent" Width="16" Background="Transparent" HorizontalAlignment="Right" Margin="0,1,16,0" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch">
                    <Button.Content>
                        <Image>
                            <Image.Source>
                                <BitmapImage UriSource="/Assets/Icone_v2/x.png" DecodePixelWidth="40" DecodePixelHeight="40"/>
                            </Image.Source>
                        </Image>
                    </Button.Content>
                </Button>
                <Button Cursor="Hand" Click="UpBotton_Click" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" Height="16" Padding="0,0,0,0" BorderBrush="Transparent" Width="16" Background="Transparent" HorizontalAlignment="Right" Margin="0,1,35,0" RenderTransformOrigin="0.5,0.5">
                    <Button.Content>
                        <Image RenderTransformOrigin="0.5,0.5">
                            <Image.RenderTransform>
                                <TransformGroup>
                                    <ScaleTransform/>
                                    <SkewTransform/>
                                    <RotateTransform Angle="-180"/>
                                    <TranslateTransform/>
                                </TransformGroup>
                            </Image.RenderTransform>
                            <Image.Source>
                                <BitmapImage UriSource="/Assets/Icone_v2/Arrow.png" DecodePixelWidth="40" DecodePixelHeight="40"/>
                            </Image.Source>
                        </Image>
                    </Button.Content>
                </Button>
                <Button x:Name="Down" Click="Down_Click" Cursor="Hand" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" Height="16" Padding="0,0,0,0" BorderBrush="Transparent" Width="16" Background="Transparent" HorizontalAlignment="Right" Margin="0,1,55,0">
                    <Button.Content>
                        <Image>
                            <Image.Source>
                                <BitmapImage UriSource="/Assets/Icone_v2/Arrow.png" DecodePixelWidth="40" DecodePixelHeight="40"/>
                            </Image.Source>
                        </Image>
                    </Button.Content>
                </Button>
                <Button Cursor="Hand" Background="Transparent" Click="ServerButton_Click" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch" Height="15" Padding="0,0,0,0" BorderBrush="Transparent" Width="15" HorizontalAlignment="Right" Margin="0,1,75,0">
                    <Button.Content>
                        <Image>
                            <Image.Source>
                                <BitmapImage UriSource="/Assets/Icone_v2/FileIcone.png" DecodePixelWidth="40" DecodePixelHeight="40"/>
                            </Image.Source>
                        </Image>
                    </Button.Content>
                </Button>
            </Grid>
            <Grid x:Name="ListViewGrid" Grid.Row="1" Margin="16,0,5,0">
                <ListBox ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.HorizontalScrollBarVisibility="Hidden" BorderThickness="0" x:Name="RegExList" Background="Transparent" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" HorizontalAlignment="Center" Height="215" VerticalAlignment="Top" SelectionChanged="RegExList_SelectionChanged">

                </ListBox>
            </Grid>

        </Grid>

    </Grid>
</Page>
