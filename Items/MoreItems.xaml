<ListBoxItem
    x:Class="VoiceX.Items.MoreItems"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VoiceX.Items"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"  BorderThickness="0" Padding="0, 0, 0, 0" Margin="0, 10, 0,0" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch">

    <Grid Background="White">
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Cursor="Hand" Background="Transparent" Height="17" Width="84" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        <Button BorderThickness="0" Height="19" Width="84"  MouseEnter="More_MouseEnter" MouseLeave="More_MouseLeave" Cursor="Hand" Background="Transparent" x:Name="More" Click="More_Click">
            <Button.Content>
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="TextColor" Text="Show more" Foreground="#FF8761F6" FontFamily="Nunito Sans 10pt Normal" FontSize="14"/>
                    <Polyline x:Name="AngleColor" Stroke="#FF8761F6" Points="0, 5, 5, 0, 10, 5" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5" Margin="3,2,0,0" >
                        <Polyline.RenderTransform>
                            <TransformGroup>
                                <ScaleTransform/>
                                <SkewTransform/>
                                <RotateTransform Angle="180"/>
                                <TranslateTransform/>
                            </TransformGroup>
                        </Polyline.RenderTransform>

                    </Polyline>
                </StackPanel>
            </Button.Content>
        </Button>
    </Grid>
</ListBoxItem>
